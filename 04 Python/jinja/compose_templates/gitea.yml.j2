services:
  gitea:
    image: gitea/gitea
    environment:
      - USER_UID={{ user_uid }}
      - USER_GID={{ user_gid }}
      - DB_PASSWORD={{ db_password }}
    ports:
      - "{{ web_port_gitea }}:3000"
      - "{{ ssh_port }}:22"
      - "gitea/{{ folderdata }}:22"

